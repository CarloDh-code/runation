<div class='game-card-show'>
  <h1> <%= @game.name %> </h1>
  <p> <%= (@game.end_date - Date.today).to_i %> days left</p>

    <div id="map" style="width: 100%; height: 400px;"
      data-controller="map"
      data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"
      data-map-markers-value="<%= @markers.to_json %>">
    </div>

</div>
